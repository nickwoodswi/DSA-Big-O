What is the Big O of the following algorithm? Explain your answer.

function isEven(value) {
    if (value % 2 === 0) {
        return true;
    }
    else
        return false;
    }
}

This is O(1) constant time complexity. The function performs once regardless of the input size.
